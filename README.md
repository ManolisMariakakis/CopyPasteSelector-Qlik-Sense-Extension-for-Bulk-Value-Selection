# CopyPasteSelector-Qlik-Sense-Extension-for-Bulk-Value-Selection
This extension provides a **textarea** where users can **paste or type multiple values** (one per line).   When clicking the **“Select”** button, the extension searches for these values in the chosen field and performs the selection automatically in the current state.
